# X-HEEP programmer PMOD - JTAG channel for CVA6 debug module.
# Variant for manual OpenOCD experiments: initialize the transport and target,
# but do not issue halt automatically.

adapter driver ftdi
ftdi vid_pid 0x0403 0x6011
ftdi channel 0

# ADBUS0=TCK out, ADBUS1=TDI out, ADBUS2=TDO in, ADBUS3=TMS out.
ftdi layout_init 0x0008 0x000b

transport select jtag
adapter speed 100

set _CHIPNAME riscv
jtag newtap $_CHIPNAME cpu -irlen 5

set _TARGETNAME $_CHIPNAME.cpu
target create $_TARGETNAME riscv -chain-position $_TARGETNAME -coreid 0

riscv set_reset_timeout_sec 120
riscv set_command_timeout_sec 120
riscv set_prefer_sba off

init
echo "OpenOCD initialized without automatic halt"
